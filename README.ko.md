[English](./README.md) | [한국어](./README.ko.md)

---

# PRIVON

**PRIVON — AI를 쓰기 전, 개인정보 보호를 켜다.**

> PRIVON 0.2.0-beta는 공개 베타입니다. 실사용 환경에서의 검증을 위해 배포되며, 1.0 수준의
> 안정성을 보장하지는 않습니다.

---

## PRIVON 0.2.0-beta 소개

PRIVON은 Windows용 로컬 우선(local-first) 개인정보 보호 유틸리티입니다.

- 클립보드에 복사한 텍스트에 전화번호·주민등록번호 등 지원되는 개인정보 패턴이 포함되어
  있으면, 사용자가 ChatGPT에 직접 붙여넣기 **전에** 클립보드 안에서 로컬로 감지·보호합니다 —
  붙여넣는 시점에 ChatGPT Windows Desktop이 활성 창이기만 하면, 다른 프로그램을 사용하는
  동안 미리 복사해 둔 텍스트에 대해서도 동일하게 적용됩니다.
- 모든 처리는 사용자의 PC 안에서만 일어납니다. 클립보드 내용이 외부 서버로 전송되지
  않습니다.
- 0.2도 계속해서 **한국어(KR) 사용 환경**과 **클립보드 경로**를 우선 지원합니다. 지원되는
  보호 대상은 여전히 **ChatGPT Windows Desktop뿐**입니다 — ChatGPT 웹(브라우저) 버전과 다른
  애플리케이션은 공식 보호 대상이 아니며, PRIVON은 그 범위 밖에서는 평소의 클립보드 사용을
  최대한 방해하지 않는 것을 목표로 합니다.

---

## 30초 시작하기

개발 도구나 명령어 없이, 아래 단계만으로 바로 사용할 수 있습니다.

1. GitHub Release 페이지에서 최신 `PRIVON-*-win-x64.zip` 파일을 내려받습니다.
2. 내려받은 zip 파일을 마우스 오른쪽 버튼으로 클릭 → **압축 풀기(Extract All)** 를 선택합니다.
3. 압축이 풀린 폴더에서 `PRIVON.exe` 를 더블클릭해 실행합니다.
4. 화면 오른쪽 아래 알림 영역(트레이)에 PRIVON 아이콘이 나타나면 정상적으로 실행된
   것입니다.
5. 이제 ChatGPT를 평소처럼 사용하세요. ChatGPT Windows Desktop이 현재(또는 방금) 활성 창이
   되면, PRIVON이 그 시점의 클립보드 내용을 확인하고 필요한 경우 붙여넣기 전에 보호
   처리합니다 — 조금 전에 다른 애플리케이션을 사용하는 동안 복사해 둔 텍스트라도 동일하게
   적용됩니다.

> PRIVON은 지금 어떤 창이 활성화되어 있는지(예: ChatGPT Windows Desktop인지 아닌지)를
> 기준으로만 동작합니다. 웹 브라우저의 ChatGPT를 포함해 다른 애플리케이션을 사용 중일
> 때는 클립보드를 읽거나 바꾸지 않습니다.

---

## 예시

아래는 실제 개인정보가 아닌 **가상의 예시**입니다.

**복사한 원문**

```
문의 전화 010-1234-5678
```

**PRIVON이 보호한 클립보드 내용**

```
문의 전화 [전화번호1]
```

붙여넣기 전에 클립보드 자체가 바뀌므로, ChatGPT에는 원문 전화번호가 아닌 보호된 형태로
전달됩니다.

---

## 0.2 지원 범위

- Windows 10 / 11 (64비트, win-x64)
- 대상 애플리케이션: **ChatGPT Windows Desktop** (ChatGPT 웹/브라우저 버전은 지원 대상이
  **아닙니다**)
- 한국어(KR) 우선 지원
- 클립보드 경로(Clipboard-first) 우선 지원 — 이제 ChatGPT로 전환하기 전에 복사해 둔
  내용도 포함
- 모든 처리는 로컬에서 수행
- 회원가입 불필요

---

## 지원하는 개인정보 종류

현재 0.2가 감지·보호를 시도하는 항목은 다음과 같습니다.

- 전화번호
- 이메일
- 주민등록번호
- 카드번호
- 계좌번호
- 시크릿/토큰처럼 보이는 값 (API 키 등)
- IP 주소
- MAC 주소
- GPS/위치 좌표

> 위 항목이라도 문맥에 따라 놓치거나 다르게 판단될 수 있습니다. PRIVON은 "완벽한 탐지"를
> 목표로 하지 않으며, 아래 [0.2.0-beta가 보장하지 않는 것](#020-beta가-보장하지-않는-것)을 함께
> 확인해 주세요.

---

## 확인이 필요한 경우 (NeedsDecision)

민감도가 더 높다고 판단되는 값은 PRIVON이 곧바로 치환하지 않고, 사용자에게 확인 창을 띄워
직접 결정하도록 합니다.

0.2에서 이 확인 창이 제공하는 동작은 **모두 보호(Protect All)** 한 가지뿐입니다. 버튼을
누르면 확인이 필요한 항목이 모두 보호된 뒤 창이 닫힙니다.

---

## 0.2.0-beta가 보장하지 않는 것

PRIVON 0.2.0-beta는 다음을 보장하거나 주장하지 않습니다.

- ChatGPT 웹(브라우저) 버전이나 그 밖의 다른 애플리케이션에 대한 보호. 0.2에서 지원하는
  보호 대상은 **ChatGPT Windows Desktop뿐**입니다.
- 완전한 익명화(anonymization)
- 개인정보보호 규제 준수 인증
- 지원 대상으로 명시된 것 이외의 모든 식별정보에 대한 탐지
- ChatGPT와의 네트워크 통신 가로채기/감시
- 메시지 자동 전송
- 키보드로 직접 입력하는 경로(direct composer typing)에 대한 보호 — 0.2가 공식적으로
  보장하는 경로는 **클립보드 경로**입니다.
- "붙여넣은 내용이 실제로 보호본과 일치하는지" 확인하는 내부 검증 기능은 존재하지만, 0.2의
  공개 기능으로 노출되어 있지 않습니다.

---

## 프라이버시 설계

현재 코드와 테스트로 검증된 사실만 기재합니다.

- 모든 탐지·보호 처리는 로컬에서 수행됩니다.
- 원격 텔레메트리(telemetry)를 전송하지 않습니다.
- 사용 분석(analytics)을 수집하지 않습니다.
- 서드파티 크래시 리포트 SDK를 사용하지 않습니다.
- 진단 로그에 원문 개인정보(raw PII)를 기록하지 않으며, 진단 로그 자체가 **기본값으로
  꺼져** 있습니다. 일반 사용자가 실행하면 아무 로그 파일도 생성되지 않습니다.
- 로컬 상태(신뢰 목록, 예외 목록 등)는 `%LocalAppData%\PRIVON` 아래 암호화되어 저장됩니다.
- 구현된 범위 내에서, Windows 세션 잠금(Win+L) 시 처리 중이던 민감한 메모리 상태(진행 중인
  확인 대기 상태 등)를 즉시 폐기합니다.

---

## Windows SmartScreen 경고

PRIVON 0.2.0-beta는 아직 코드 서명이 되어 있지 않습니다. 이 때문에 Windows가 "알 수 없는
게시자" 관련 SmartScreen 경고를 표시할 수 있습니다. 이는 정상적인 동작이며, 코드 서명 미적용
소프트웨어에서 흔히 나타나는 안내입니다.

**공식 GitHub Release에서 받은 파일**이라면, 다음과 같이 진행할 수 있습니다.

1. 경고 창에서 **추가 정보(More info)** 를 클릭합니다.
2. **실행(Run anyway)** 을 클릭합니다.

Windows Defender나 SmartScreen 자체를 비활성화할 필요는 없으며, 권장하지도 않습니다.

---

## 종료 방법

작업 표시줄 알림 영역의 PRIVON 트레이 아이콘을 마우스 오른쪽 버튼으로 클릭한 뒤 **Exit** 를
선택하면 프로그램이 종료됩니다.

---

## 자동 시작 (선택 사항)

PRIVON은 Windows에 로그인할 때 자동으로 실행되도록 선택적으로 설정할 수 있습니다.

- **기본값은 꺼짐(off)** 입니다 — 사용자가 직접 켜지 않는 한 PRIVON은 스스로 Windows
  시작프로그램에 등록되지 않습니다.
- PRIVON 트레이 아이콘 메뉴에서 언제든지 켜고 끌 수 있습니다.
- 켜면 현재 사용 중인 Windows 사용자 계정에 대해서만 등록됩니다 — 관리자 권한이 필요하지
  않으며, 같은 컴퓨터의 다른 사용자 계정에는 영향을 주지 않습니다.
- 자동 시작을 켠 상태에서 압축을 푼 PRIVON 폴더를 나중에 옮기거나 이름을 바꾸면, PRIVON은
  자동 시작이 여전히 정상 동작하는 것처럼 잘못 표시하지 않습니다 — 안전한 방향으로 실패하여
  자동 시작이 꺼진 것으로 표시합니다. 새 위치에서 다시 켜면 다시 등록됩니다.

PRIVON이 이미 실행 중인 상태에서 다시 실행해도 중복 인스턴스가 생기지 않습니다 — 나중에
실행한 쪽은 곧바로 종료되고, 기존에 실행 중이던 PRIVON(트레이 아이콘, 클립보드 보호)은
그대로 계속 동작합니다.

---

## 베타 상태 및 피드백

PRIVON 0.2.0-beta는 실사용 환경에서의 검증을 위한 **공개 베타**입니다. 아직 1.0 수준의
안정성을 보장하지 않으며, 예상하지 못한 동작이 발견될 수 있습니다. 문제를 발견하시면 GitHub
Issue 또는 Discussions 탭으로 알려주세요.

---

## 소스에서 직접 빌드하기

> 아래는 일반 사용자를 위한 절차가 아닙니다. 직접 빌드하려는 개발자를 위한 절차입니다.

### 요구 사항

- Windows 10/11
- .NET 10 SDK

### 복원 / 빌드 / 테스트

```powershell
dotnet restore PRIVON.slnx
dotnet build PRIVON.slnx -c Release
dotnet test PRIVON.slnx -c Release
```

### 공개 배포용 게시(publish)

```powershell
dotnet publish src/Privon.App/Privon.App.csproj `
  -c Release `
  -r win-x64 `
  --self-contained true `
  -p:PublishSingleFile=true `
  -p:PublishReadyToRun=false `
  -p:DebugType=None `
  -o publish/win-x64
```

반복 가능한 릴리스 패키징(zip + sha256 생성 포함)은 `tools/publish-release.ps1` 스크립트로도
수행할 수 있습니다.

```powershell
.\tools\publish-release.ps1
```

---

## 검증 현황

- Debug/Release 두 빌드 구성 모두에서 자동화 테스트 1,880개 전체 통과(GREEN), 빌드 경고
  0개·오류 0개
- 실제 Windows 클립보드를 대상으로 한 320회 실측 시도: 낮음/중간 위험도 개인정보는 보호가
  필요한 모든 경우에 자동으로 보호·검증되었고, 높은 위험도(NeedsDecision) 값은 자동으로
  조용히 보호되지 않고 사용자 확인을 위해 그대로 남겨짐이 정상적으로 확인됨
- 실제 Windows + 실제 ChatGPT Windows Desktop 환경에서의 클립보드 보호 동작 확인(smoke
  test) 통과 — 다른 애플리케이션에서 먼저 복사한 뒤 ChatGPT로 전환하는 경우 포함
- 웹 브라우저의 ChatGPT를 포함해 지원 대상이 아닌 다른 애플리케이션에는 개입하지 않음을
  실제 환경에서 확인 통과
- Windows 세션 잠금(Win+L) 관련 동작 확인(smoke test) 통과
- 선택적 자동 시작 기능을 실제 Windows 재부팅/로그인으로 켠 상태·끈 상태 양쪽 모두 확인
  통과
- PRIVON이 이미 실행 중일 때 다시 실행해도 중복 인스턴스가 생기지 않음을 확인 통과
- Release용 publish 산출물 실행 확인(smoke test) 통과

이 검증은 테스트된 범위와 조건 안에서의 결과이며, "모든 환경에서 완벽하게 동작함"을
의미하지 않습니다.

---

## 공개 기록

- 최초 소스 공개: 2026년 8월 22일
- 최초 공개 베타 Release: 2026년 8월 22일 — `v0.1.0-beta`
- 최초 공개 소스 commit: `98a5c2d`
- `v0.1.0-beta` tag 대상 commit: `1380d44`
- 배포 ZIP SHA-256: `d1417848847f5f2140bbf619662973ef69c86dff65ecd45607db8acf7699281f`

---

## 라이선스

PRIVON 소스 코드는 GitHub에서 공개적으로 열람할 수 있으며, **PolyForm Shield 1.0.0**
라이선스로 배포됩니다.

- 이는 OSI(Open Source Initiative) 공인 오픈소스 라이선스가 아니라 **소스공개형
  (source-available)** 라이선스입니다. "오픈소스"라는 표현은 정확하지 않습니다.
- 일반적인 사용, 소스 코드 열람, 수정, 그리고 GitHub 상의 협업(포크, Pull Request 등)은
  라이선스가 허용하는 범위 안에서 가능합니다.
- 다만 PRIVON을 이용해 PRIVON(또는 라이선서가 PRIVON을 이용해 제공하는 다른 제품)과
  **경쟁하는 제품이나 서비스를 제공하는 행위**는 라이선스에 의해 제한됩니다.
- 정확하고 구속력 있는 전체 조항은 반드시 저장소 루트의 [`LICENSE`](./LICENSE) 파일을
  확인해 주세요. 이 설명은 요약일 뿐이며, 실제 권리·의무는 `LICENSE` 원문이 우선합니다.
